# P3-005 CI Security and Release Provenance Report

## Current Status

- Task: P3-005 CI Security and Release Provenance
- Formal version: `v1.1.0`
- Candidate version: not assigned
- Implementation: COMPLETE WITH LOCAL EVIDENCE
- Verification status: BLOCKED
- Blocking gate: Docker compose smoke cannot run because no Docker-compatible runtime is installed in the authorized local environment, and push/remote workflow dispatch is not authorized.
- Product runtime changes: `0`
- Candidate, tag, Release, or published attestation created: `0`

The blocker is an incomplete mandatory environment gate, not a known product or security implementation regression. The confirmed CONDITIONAL cases do not include missing Docker evidence, so this task is not downgraded to CONDITIONAL.

## Workflow Inventory

| Workflow | Triggers | Jobs | Permissions | Artifacts / release behavior |
| --- | --- | --- | --- | --- |
| `.github/workflows/ci.yml` | pull request; push to `main`; push to `v*`; manual dispatch | Backend pytest; Frontend build; Docker compose smoke; Workflow policy; Dependency audit; Secret audit; SBOM validation; Release evidence dry-run | workflow and every job explicitly `contents: read` | no uploaded artifacts; no OIDC; no write permission; release evidence is no-publish and exact-tag gated |

The existing Backend and Frontend job display names are preserved. Docker continues to run only for manual/tag events and remains skipped for ordinary PR/main events.

## Immutable Actions And Permissions

| Action | Version | Immutable SHA |
| --- | --- | --- |
| `actions/checkout` | `v4.3.1` | `34e114876b0b11c390a56381ad16ebd13914f8d5` |
| `actions/setup-python` | `v5.6.0` | `a26af69be951a213d495a4c3e4e4022e16d87065` |
| `actions/setup-node` | `v4.4.0` | `49933ea5288caeca8642d1e84afbd3f7d6820020` |

The pins were resolved from official Action repository refs on 2026-07-18 and are recorded in `.github/security/action-pins.json`. CI tool installation is bounded by `.github/security/tool-versions.json`; `uv`, `pip-audit`, and `check-jsonschema` are exact-version pinned.

Measured policy results:

- workflows: `1`
- third-party Action uses: `16`
- `third_party_action_full_sha_pin_rate = 1.0`
- `workflow_permissions_explicit_rate = 1.0`
- ordinary CI write/OIDC/release permissions: `0`

## Dependency And Secret Gates

Dependency inventory and audit:

- Python locked packages: `40` (`24` runtime, `16` development)
- npm locked packages: `217` (`213` runtime, `4` development)
- scanners: pinned `pip-audit`, `npm audit`, and independent OSV querybatch
- merged findings: `0`
- blocked findings: `0`
- suppressions: `0`
- result: PASS

Secret audit:

- current repository files and bounded Git history scanned
- logs contain rule ID, path, line, irreversible fingerprint, and classification only
- reported findings: `0`
- credible findings: `0`
- suppressions: `0`
- result: PASS

Scanner unavailability, malformed output, unresolved severity, expired/unmatched suppression, and credible secret classification fail closed.

## SBOM Evidence

Three deterministic CycloneDX 1.6 JSON SBOMs were generated twice in independent temporary directories and removed after validation.

| SBOM | Components | Dependencies | SHA-256 |
| --- | ---: | ---: | --- |
| Backend | 40 | 41 | `ac79dd3dff096346dc002707f7582e6c79785046c5c6e0287e84b54b9a7fdbb5` |
| Frontend | 217 | 218 | `a37165d1573d8fdd2f9ec3482678848bf29e4e26d8bb3703a508317d67b82a7d` |
| Combined | 259 | 260 | `566d68e7b130832df9d7b1f9ad297d46c01864af790cde44dd0b11d68dde548f` |

- official CycloneDX 1.6 schema validation: PASS
- Backend lock coverage: PASS
- Frontend lock coverage: PASS
- repeated-build byte identity: PASS
- combined size: `222793` bytes (limit `5242880`)
- forbidden runtime/private artifact count: `0`
- generated SBOMs tracked: `0`

The local fingerprints describe pre-commit verification at the recorded baseline. Exact-commit SBOMs are regenerated by CI and are not committed.

## Release Provenance Boundary

The release-evidence job depends on Backend, Frontend, Docker, workflow policy, dependency, secret, and SBOM jobs. It is eligible only for `refs/tags/v*` reached by tag push or manual dispatch against the tag, and it has `contents: read` only.

Local no-publish evidence against `v1.1.0` verified:

- tag target: `3efbe2a792a9853f1bac456f0287c3b5b62713ce`
- eligible artifacts: `3` SBOM JSON files
- evidence digest verification: PASS
- branch `would_authorize_publish=false`
- `publish_authorized=false`
- publication or external write count: `0`

## Compatibility Evidence

- focused security tests: `10 passed`
- focused release tests: `2 passed`
- Backend: `530 passed, 3 skipped`
- Frontend `npm ci`: PASS; npm reported `0 vulnerabilities`
- Frontend Next.js production build: PASS
- workflow policy: PASS
- suppression validation: PASS
- dependency audit: PASS
- secret audit: PASS
- SBOM validation and reproducibility: PASS
- release-evidence dry-run and offline verification: PASS
- Docker compose smoke: NOT RUN, BLOCKING ENVIRONMENT LIMITATION

No Backend, Frontend, product dependency, Article contract, legacy API, `/v1.1` API, Provider default, corpus, Graph, RAG, or storage implementation file was modified.

## Dependabot And Operations

`.github/dependabot.yml` proposes bounded weekly updates for Backend Python and Frontend npm dependencies. It opens at most five pull requests per ecosystem and has no auto-merge, tag, or release behavior.

Operational documents:

- `docs/CI_SECURITY_TRIAGE_SOP.md`
- `docs/ACTION_PIN_UPDATE_SOP.md`
- `docs/SBOM_VERIFICATION_SOP.md`
- `docs/RELEASE_PROVENANCE_VERIFICATION_SOP.md`
- `docs/BRANCH_PROTECTION_GUIDANCE.md`

Branch protection remains guidance only; no repository-setting API was called.

## Artifact And Privacy Audit

No generated SBOM, release evidence, scanner output/cache, `.env`, credential, corpus/Article export, PDF, HTML/image, database, archive, Graph/RAG/FAISS data, trace/profile, runtime store, private Zotero/user data, or local absolute path is intended for commit. Temporary evidence was created outside the repository and cleaned.

## Blocker And Closure Path

Required Docker evidence is unavailable because the environment reports command-not-found for `docker`, `docker-compose`, `podman`, and `nerdctl`. Under the confirmed local-only/no-push boundary there is no authorized remote workflow path to replace this gate.

To close P3-005:

1. Run `docker compose up --build -d` and the existing Backend/Frontend smoke checks in a Docker-capable environment, then tear down volumes and orphans; or separately authorize a remote manual workflow against the exact completion commit.
2. Confirm all Docker checks pass without changing product runtime behavior.
3. Re-run final artifact, secret, Git, and changed-path audits.
4. Record PASS and separately audit any push authorization.

## Decision

- Status: BLOCKED
- Reason: mandatory Docker compose smoke evidence is unavailable in the authorized environment
- Push: NOT PERFORMED / NOT AUTHORIZED
- Candidate/tag/Release: NOT CREATED
- Next action: obtain Docker-capable validation evidence, then close P3-005 and separately authorize push.
