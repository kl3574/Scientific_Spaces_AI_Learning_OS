# P3-036.1 Reference Candidate Focus Lifecycle

Status: IMPLEMENTATION PASS / PARENT CLOSURE BLOCKED; parent P3-036 remains open.

The separate closure commit `d28fec6` failed CI `34196981094` at Graph map
selection, not the candidate-filter focus assertion. Its six other required
jobs pass. Parent report section 17 tracks this new failure; candidate-focus
implementation is unchanged and no later task has started.

Local candidate-focus validation: PASS (16 browser checks, 32 CPU-throttled
route switches, 12 retry cases; two independent final reviews).
Prior full invocation: failed at Reader JavaScript chunk loading before the
new helper executes. Replacement full local gate: PASS, 3/3 iterations with
243 checks each, final network/error audits and restart persistence PASS.
Backend 600/4 skipped, Frontend 141 and production build PASS. Repository
safety gates and the additional independent diff review pass. Implementation
commit/push and exact-SHA implementation CI pass; parent closure remains pending.
The historical Reader chunk error is retained as unexplained risk.
See section 15 of the parent evidence report for current diagnostic evidence.

Implementation commit: `472350ede8bc20651928ebbc5d88abb206ee6b47`, pushed.
Exact-SHA CI: `34194053415`, SUCCESS. Backend, Frontend and required security
jobs PASS; ordinary Product E2E passes 3/3 x 243 checks with restart persistence,
zero unexpected console/page errors and zero external requests. Uploaded
artifacts: 0. Neither task is closed before the separate docs-only closure
commit passes its own exact-SHA CI.

## Evidence And Authorization

Baseline: `d1e26828fe42f2f1c605973b034295c5b6ee80af`, clean `main`.
Exact-SHA CI `34188149037` fails the existing Matched-focus assertion.
Backend, Frontend build, dependency, workflow, secret and SBOM jobs pass.
The final network audit was not reached; its success is not inferred.

Fresh isolated Chromium reproduced the same lifecycle defect without the
identity-error/retry prelude: CPU4 All and CPU8 Matched focus attempts execute
while the button is disabled, consume the intent, and leave Shell main as the
eventual focus owner. CPU1 passed 12 switches; CPU4 failed at switch 10 and
CPU8 at switch 5. External requests and unexpected browser errors: zero.

The product owner's standing instruction authorizes independent review and
automatic bounded GUI improvements. An independent reviewer confirmed this
product repair lies outside the original nine-component allowlist. This
explicit subtask extends only the candidate-filter focus lifecycle; it does
not reinterpret the old evidence-only repair as product authorization.

## Scope

Allowed: `frontend/src/components/ZoteroReferenceReview.tsx`, candidate focus
intent helpers in `frontend/src/lib/referenceReview.ts`,
`frontend/tests/referenceReview.test.ts`, `scripts/e2e/run_product_e2e.py`,
this task, parent task/report, `alignment.md`, `docs/tasks/CURRENT_TASK.md`,
and existing project status/roadmap/README pointers as needed.

No other reference focus effect or Shell redesign. No Backend, frozen M1,
API, data/schema, matching, dependency, lockfile, workflow, source network,
private Zotero, real/paid Provider, candidate, tag or Release changes.
No downloaded data, screenshots, traces, profiles, caches or secrets in Git.

## Contract

1. Focus the enabled, connected current candidate filter after navigation.
2. A cancelled animation frame must not consume a still-current intent.
3. Newer user interaction, reference, route, modal or operation supersedes old
   focus work; stale same-filter work cannot consume a newer request.
4. Loaded, empty and error/retry states retain truthful feedback and focus.
5. Preserve requests, data writes, URL/history, original E2E assertions and
   strict HTTP auditing. Do not replace readiness with sleeps or larger timeouts.

## Execution And Verification

Record RED, add regression, repair the bounded lifecycle, then independently
review implementation and public-browser evidence. Verify desktop/mobile and
CPU4/8 route switches, pending retry success/error, newer input/modal/route,
and final focus visibility. Run `npm --prefix frontend run test:references`,
the other existing focused suites, `npm --prefix frontend run build`,
`uv run --offline --project backend --extra dev pytest -q`, and three complete
Product E2E runs. Run repository safety gates before commit.

PASS requires all local gates plus exact-SHA main CI. Failures keep both this
subtask and parent open; diagnosis must not weaken the test contract. Stop on
unknown worktree drift, artifacts/secrets, or required prohibited changes.

Implementation commit: `fix: preserve reference candidate filter focus`.
Non-force push and exact-SHA CI are authorized; parent docs-only closure and
its separate exact-SHA CI remain required. No tag, Release or force operation.
