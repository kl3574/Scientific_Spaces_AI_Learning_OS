# P3-024.1 Bootstrap Hydration Compatibility

Status: LOCAL VERIFICATION PASS / INTEGRATION CI PENDING

## Evidence And Authority

The security-required Next 15.5.24 update skips the existing production bootstrap
workaround, which admits only 15.5.21. Installed bundled production React remains
19.2.0-canary-0bdb9206-20250818; App Router still invokes hydrateRoot inside
startTransition. The original full E2E fails with React 418/HTML.

A corrected, independently reviewed exact-prefix diagnostic reproduces one
418 on Dashboard before Reader entry, with complete original error ledgers,
no unrelated/external errors, stable bindings and successful cleanup. A fresh
Reader-only control is negative. This establishes a behavioral RED and a skipped
guard, not the framework's internal causal mechanism or prefix necessity.

The owner's standing automatic platform/GUI direction and independent review
approve a separately bounded exact-version counterfactual. No further generic
confirmation is required. P3-039 remains OPEN / DEFERRED and unrelated.

## Allowed Changes

- frontend/src/lib/bootstrapHydration.ts: exact Next literal and its comment only
- frontend/tests/bootstrapHydration.test.ts: version-binding and mismatch tests
- this canonical task and docs/P3_024_1_BOOTSTRAP_HYDRATION_COMPATIBILITY_REPORT.md
- already-allowed current governance and P3-005.3/P3-005.4 evidence paths

These four new paths extend the preserved integration candidate from 24 to 28.
Retain every production guard, exact React version, one-shot execution, descriptor
restoration, later-call delegation and microtask expiry. Unsupported contexts
still skip. No range/wildcard or automatic version-derived guard is permitted.
No AppShell/Reader/Graph logic, dependency, workflow, audit/acceptance policy,
Backend/M1/API/schema, source/private/paid access, tag or Release change.

## Execution And Acceptance

1. Add a RED test binding the literal to manifest, root lock declaration, locked
   and installed Next versions, and Next's bundled production React export.
   Root React 19.0.0 and bundled React's versionless package metadata are not
   substitutes. Preserve mismatch coverage including the former Next version.
2. Rebind only the exact Next literal/comment to 15.5.24. All existing tests pass.
3. Run the unchanged corrected prefix harness as a matched counterfactual using
   the same owned build process, environment, ports 18000/3000, fixture and
   assertions. Record expected between-build differences and within-run
   immutability. Remaining 418 or invalid observation stops the affected gate.
4. Only a passing counterpart admits hard-load stress: preserve the earlier
   P3-024 recipe (Graph normal 500, Graph cache-disabled 200, Graph 390x844,
   320x844 and 720x450 100 each, Articles 200), plus Dashboard 100 and Reader 100
   split equally between desktop and mobile. Total 1400 hard loads. Require
   correct heading/shell hydration, bounded read-only version/marker evidence,
   stable per-route SSR hashes, no unexpected console/page/external errors,
   unchanged inputs and complete cleanup. No HTML/body/image/trace export.
5. Seven original provenance cases, three unchanged complete Product E2E runs,
   restart persistence, all Backend/Frontend/build/security gates and final
   independent review must pass before the existing integration commit and
   non-force publication. Exact-SHA main CI still precedes closure. No partially
   verified commit, blind rerun, diagnostic result substituted for full E2E,
   policy waiver, or receipt-only commit loop.

Commit remains `fix: update vulnerable dependency versions` for the explicitly
reviewed combined integration. Formal version v1.1.0; candidate none. Preserve
and exclude the known frame-oracle draft; no runtime/private artifacts or secrets.
The full platform/GUI objective remains active beyond this compatibility repair.
