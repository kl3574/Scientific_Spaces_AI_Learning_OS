# P3-005.3 Dependency Security Repair

Status: PASS / CLOSED

## Closure Receipt

Replacement integration [0a203bae2f99e3c2b3223823e7d91437bc27e96a](https://github.com/kl3574/Scientific_Spaces_AI_Learning_OS/commit/0a203bae2f99e3c2b3223823e7d91437bc27e96a)
passes exact-SHA [main CI 34314910981](https://github.com/kl3574/Scientific_Spaces_AI_Learning_OS/actions/runs/34314910981),
completed SUCCESS at 2026-09-09T06:14:45Z. All seven required jobs pass.
Product E2E: three runs of 298 checks, no failed checks, restart persistence
four checks true, zero unexpected console/page errors or external requests.
Uploaded artifacts: 0. Docker/release jobs are policy-skipped, not PASS.
The prior local gates and independent final reviews remain recorded below.

Current task: [P3-041 Reader Inline Image Rendering](P3-041_READER_INLINE_IMAGE_RENDERING.md),
OPEN / IMPLEMENTATION, not completed by this prior CI. P3-039 remains
OPEN / DEFERRED, historical cause UNKNOWN. Formal version v1.1.0; candidate None.

Historical record follows: earlier pending/open gate statements describe their
pre-closure checkpoints, not current status. Original failures, requirements,
thresholds and scope are retained unchanged.

Current additional prerequisite: independently reviewed P3-024.1 Bootstrap
Hydration Compatibility. Two exact-version code/test paths and two task/report
paths extend the historical 24-path candidate to exactly 28. Its matched prefix
passes; stress 1400/1400, provenance 7/7, complete E2E 3 x 298 and restart
persistence now pass. Final receipt review and exact-SHA CI remain required.
Only that canonical exception permits the exact Next literal/comment rebind.
The port-wait and earlier scope descriptions below are historical, not current
requests to stop the user's service or restart approval.

Additional independently reviewed prerequisite: P3-005.5 Npm Dependency Ancestry.
Its canonical task permits npm-only lockfiles.py changes and two new task/report
paths, extending the preserved integration scope from 21 to exactly 24 paths.
No other parser/scanner/policy/product change is authorized. Preserve all earlier
gates, including the now-failed full browser run; no publication before repair.

Active independent test-only prerequisite: P3-005.4 Product Test Runtime
Isolation. Its canonical task explicitly permits five executable paths and two
new documents beyond this 14-path candidate, exactly 21 combined paths. The
browser test semantics and all security/publication gates remain unchanged;
only backend-port configuration and owned build/process isolation are added.
No need to stop the user's backend. The following records the original gate.

Historical entry gate: LOCAL BROWSER VALIDATION WAIT. Dependency audits, locked installs,
Backend/Frontend/build, image compatibility and full SBOM/security gates pass.
The unchanged browser runner stops before cases because an existing user-terminal
backend occupies port 8000. Preserve that service; no test waiver or publication.
The evidence report records the port and namespace diagnostics.

## Objective And Entry Evidence

Remove eight unsuppressed dependency findings blocking P3-040 without changing
application behavior, audit policy or frozen contracts. Baseline main/origin/main:
fee813b96c6940840bfab73185e9e77e3e594e34. Failed job: 102274840215 in exact-SHA
run 34290207866. The original dependency audit exits 1 in CI and locally:
PyPI 40 / npm 239, eight findings, eight blocked, zero suppressed.

P3-040 local Backend 770/4 skipped, Frontend 153, build, seven focused browser
cases and full 3 x 298 E2E pass. Its dependency CI does not. Preserve the failed
receipt and keep P3-040 open until replacement exact-SHA CI passes. P3-039
remains OPEN / DEFERRED, historical root cause UNKNOWN.

## Independently Reviewed Scope

The owner directs continued platform/GUI improvement with sub-agent review and
automatic execution. Independent review approves this separate dependency task
under the CI Security Triage SOP, not silent expansion of P3-040. No repeated
generic plan confirmation is required. Exactly these paths may change:

- backend/uv.lock
- frontend/package.json
- frontend/package-lock.json
- scripts/security/build_sbom.py
- scripts/security/tests/test_sbom.py
- docs/tasks/P3-005.3_DEPENDENCY_SECURITY_REPAIR.md
- docs/P3_005_3_DEPENDENCY_SECURITY_REPAIR_REPORT.md
- docs/P3_040_EXPANDED_PROVENANCE_RETURN_CONTINUITY_REPORT.md
- README.md
- alignment.md
- docs/00_PROJECT_STATE.md
- docs/tasks/CURRENT_TASK.md
- roadmap.md
- docs/V1_2_ROADMAP.md

Minimum aggregate officially fixed targets: Next 15.5.24, Sharp 0.35.4,
HTTPX2 and its exact HTTPCore2 dependency 2.12.0. Preserve legacy HTTPX, AnyIO,
IDNA, truststore, React and unrelated packages. Only necessary declared
transitive changes are allowed. Existing pyproject ranges suffice.

Reviewed lock-resolution allowance: retain exactly six `inBundle` records under
unchanged @tailwindcss/oxide-wasm32-wasi 4.1.17. Warm and owned cold npm
resolution produce them; the parent's integrity-verified public archive confirms
their exact names/versions. They represent already-bundled code, not a Tailwind
upgrade. Keep these records in scanner/SBOM inventory; do not hand-prune them.

Also verify a locked `npm ci` with the existing CI tool version npm 10.9.8,
without repairing the lock or removing bundled records. The exact public npm
tool download is allowed for this check; do not change global npm or CI config.

Independent necessary amendment: the original SBOM CLI fails on duplicate
`pkg:npm/tslib@2.8.1` from root/bundled occurrences. The allowlist is now 14
paths, adding only the generator and its tests. Normalize ecosystem output by
existing bom_ref: one component and dependency entry, unioned edges, runtime
precedence, consistent identity and all available nonconflicting hashes. Reject
same-algorithm hash conflicts. Preserve different versions, duplicate-free
output and source/commit bindings. Keep all 245 audit installation records.
Demonstrate regression RED/GREEN and validate original CLI/full schema output.
No combined-builder refactor, lock parser or validator change is authorized.

Public official advisories/upstream metadata, PyPI/npm exact packages, existing
public audit/SBOM endpoints, and authorized GitHub publication/CI readback are
allowed. No source-site, private registry, Zotero, corpus, real/paid Provider,
application/M1/API/schema, workflow/scanner/policy/suppression or assertion change.

## Execution And Acceptance

1. Record original CI findings and reproduce the unmodified audit locally.
2. Verify upstream affected/fixed ranges and exact registry provenance. Review
   generated lock deltas independently; use targeted resolution, not bulk upgrades.
3. Run locked installation, full Backend and Frontend tests, production build,
   and actual in-memory Sharp/Next optimizer compatibility checks.
4. Run unchanged seven-case provenance and full repeat-three E2E on the rebuilt
   dependencies, including restart persistence and strict request/error audits.
5. Run two original complete dependency audits with stable inventories, all
   three scanners operational, zero blocking findings and zero suppressions.
6. Pass existing security tests, workflow/suppression/secret policies, temporary
   SBOM generation/validation, artifact and protected-path checks.
7. Independent final diff review and all local gates precede non-force main
   publication. All seven required replacement exact-SHA CI jobs precede closure.

Commands: targeted `uv lock --project backend --upgrade-package httpx2==2.12.0
--upgrade-package httpcore2==2.12.0`; `uv sync --project backend --extra dev
--locked`; `npm --prefix frontend install --package-lock-only --ignore-scripts
--no-audit --no-fund`; `npm --prefix frontend ci --ignore-scripts --no-audit
--no-fund`; `uv run --offline --project backend --extra dev pytest -q`;
`npm --prefix frontend run test:articles`, `test:tutor`, `test:references`,
`test:graph` and `build`; unchanged browser runners and scripts/security tools.

## Safety And Delivery

Preserve/exclude the known untracked scripts/e2e/graph_frame_oracle.js. No
runtime/private artifact, secret, tag, Release, force push, history rewrite or
major upgrade. Unknown drift, incompatible behavior, unavailable fix, unrelated
package delta, forbidden artifacts/secrets or failed gates stop the affected
action for diagnosis. Do not lower UNKNOWN severity or waive a scanner failure.

Commit: `fix: update vulnerable dependency versions`. Publish non-force after
local gates and verify replacement exact-SHA main CI. Docker/release jobs keep
normal-main policy: SKIPPED, not PASS. No receipt-only commit loop or inherited
branch/release authorization from the closed P3-006 task. After security/P3-040
closure, resume reviewed GUI improvement. Formal v1.1.0; candidate none.
