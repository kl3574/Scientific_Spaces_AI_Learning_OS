# P3-005.5 Npm Dependency Ancestry

Status: LOCAL VERIFICATION PASS / INTEGRATION CI PENDING

## Objective And Authority

Independent final security review and a separate master reproduction find a
wrong cross-platform SBOM edge. The bundled wasm-runtime 1.0.7 importer binds
emnapi runtime 1.11.3 at the root instead of its nearest ancestor's 1.6.0.
Inventory/schema checks pass but cannot prove correct dependency ancestry.

The owner authorizes independently reviewed automatic platform improvement.
Independent scope review approves this separate security revision, not a change
to product scope or a waiver of the current React 418 full-browser failure.

## Scope And Contract

Add only scripts/security/lockfiles.py and this task/report beyond the previous
21-path integration candidate: exactly 24 paths. Reuse the already-allowed
scripts/security/tests/test_sbom.py, P3-005.3/P3-005.4 reports and six governance
pointers. Report: docs/P3_005_5_NPM_DEPENDENCY_ANCESTRY_REPORT.md.

- Change npm dependency resolution only; preserve the UV parser.
- Resolve the nearest legal Node ancestor, including scoped packages, stopping
  at the lock root. Never use global-name, unrelated-branch, highest-version or
  semver fallback.
- Missing required dependencies of ordinary packages, including root direct
  dependencies, raise SecurityToolError.
- Preserve existing optional omission allowances, using legal reachability
  rather than global name presence. An unrelated record cannot satisfy a
  dependency or prevent an allowed omission. Omit edges only, never inventory.
- Preserve all 245 occurrences, identities, versions, digests and scope. Keep
  distinct versions and all existing duplicate-identity merge contracts.
- No validator, scanner, policy, suppression, workflow, dependency, product,
  Backend/API/M1/schema, source/private/paid or tag/Release changes.

## Verification And Delivery

Witness RED/GREEN for the real bundled 1.6.0 versus root 1.11.3 error. Add
nearest-depth, scoped-package, root, missing-required, unreachable-optional and
unrelated-branch cases, with Node's installed ancestor-path implementation as a
read-only reference. Verify corrected frontend and combined SBOM edges as well
as unchanged inventories, component identities, hashes and scope classifications.

Run the full security suite, ordinary Backend tests, original full SBOM CLI and
schema validation in an owned temporary directory, and both original complete
dependency audits. Preserve the prior failed results and all original browser
gates. Final independent review and all combined local gates precede the existing
integration commit, `fix: update vulnerable dependency versions`, non-force push
and exact-SHA CI. No partial publication or generic confirmation loop.

Stop the affected action on unknown drift, unresolved required dependencies,
changed inventory, forbidden artifacts/secrets, necessary extra scope or required
verification failure. Preserve/exclude the existing graph_frame_oracle.js draft.
The broader platform/GUI goal stays active; this is not its completion criterion.
