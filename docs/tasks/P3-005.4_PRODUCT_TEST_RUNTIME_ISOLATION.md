# P3-005.4 Product Test Runtime Isolation

Status: LOCAL VERIFICATION PASS / INTEGRATION CI PENDING

## Objective And Authority

The continued platform/GUI goal authorizes independent review and automatic
bounded execution. The existing seven-case regression stops before any case
because a user-terminal backend occupies port 8000. Do not stop or reuse it.
An unprivileged namespace check fails; do not bypass host security policy.

Independent review accepts this separate test-only prerequisite for P3-005.3.
It replaces repeated resource/plan confirmation with isolated verification,
not a waiver or a claim that the platform goal is complete.

## Contract

- Preserve frontend origin http://127.0.0.1:3000 because unchanged backend CORS
  supports it. If port 3000 is occupied, stop without service reuse.
- Add one optional strictly validated unprivileged backend port, distinct from
  3000. Default remains 8000. Reject invalid/occupied ports without fallback.
- Startup, restart, routes, injected JavaScript and strict network audits use
  the same configuration. Preserve browser localhost versus control-plane
  127.0.0.1. Isolated audits replace port 8000; they must not also admit it.
- Build only in an owned temporary frontend copied from unchanged tracked
  source/config/package inputs, using existing locked dependencies. Compile the
  exact browser API endpoint. No product or Next configuration edit is needed.
- Exclude environment files, runtime/private data, prior builds and caches.
  Sanitize child environments, disable telemetry, perform no install/download,
  verify copy containment and input hashes, and preserve shared dependency/build
  contents. The dependency symlink does not constitute filesystem isolation.
- Bind dedicated-runner evidence to the actual temporary build. Configure the
  imported runner's function globals, not merely its runpy return dictionary.
- Preserve all UI assertions, deadlines, seven cases, full repeat-three checks,
  restart persistence and fail-closed network/error audits. Own and clean every
  build/start/restart process group, temporary build and synthetic runtime.

## Allowed Changes

- scripts/e2e/run_product_e2e.py
- scripts/e2e/check_graph_provenance_return.py
- scripts/e2e/product_test_runtime.py (new)
- backend/tests/test_e2e_runtime_isolation.py (new)
- backend/tests/test_graph_provenance_return.py
- this task and docs/P3_005_4_PRODUCT_TEST_RUNTIME_ISOLATION_REPORT.md
- existing P3-005.3 task/report and six governance pointers: README.md,
  alignment.md, docs/00_PROJECT_STATE.md, docs/tasks/CURRENT_TASK.md, roadmap.md,
  docs/V1_2_ROADMAP.md

Preserve the independently reviewed 14-path P3-005.3 candidate and its P3-040
CI receipt. The combined integration candidate is exactly 21 paths. Preserve
and exclude scripts/e2e/graph_frame_oracle.js. No other product, dependency,
backend/M1/API/schema, workflow, policy, assertion or data changes.

## Verification And Delivery

1. Offline RED/GREEN contracts: defaults, invalid/occupied ports, cross-port
   rejection, every backend predicate, actual build bindings, excluded inputs,
   stable shared files, and build/start/restart/teardown failures. A failure must
   not return PASS or signal/read an unrelated service.
2. Independent pre-browser diff review. No browser work before this gate.
3. Run seven cases and original full repeat-three on an isolated production
   build with strict audits, persistence and cleanup. The existing backend must
   remain untouched and available. Run full ordinary Backend/Frontend/build and
   P3-005.3 safety/security gates on the final combined candidate.
4. Independent final integration review precedes commit/publication. Include
   this explicitly recorded prerequisite in the P3-005.3 integration commit
   `fix: update vulnerable dependency versions`; no partially validated push.
5. Non-force main publication and all required exact-SHA CI jobs are still
   required. CI continues to exercise the unchanged default invocation. Docker
   and release jobs remain policy-skipped on ordinary main pushes.

No source-site/private Zotero/real or paid Provider access, credential use,
runtime artifacts, destructive Git, candidate/tag/Release or generic owner
confirmation. Stop the affected action on unknown worktree drift, unsafe copy
or cleanup, changed shared state, required failure or necessary forbidden scope.
After these prerequisite and security gates, resume P3-040 closure and platform
and GUI improvement. Formal version v1.1.0; candidate none.
