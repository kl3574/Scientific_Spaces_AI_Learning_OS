# P3-034.1 Guided Reader History Focus Diagnostics

Status: LOCAL VERIFICATION PASS / PUBLICATION CI PENDING

Local Backend 1184/4 skipped, Frontend 179, focused contracts 37, owned build,
original 3 x 298/restart/image and native36/component6 profiles pass. Audits,
bindings and cleanup pass. Final receipt/safety review precedes publication;
all seven required exact-SHA CI jobs remain necessary. This is diagnostic
qualification, not a Reader repair or parent closure.

## Purpose And Authority

Collect missing evidence at the actual guided Reader hashless-Back focus failure
without changing product behavior or acceptance. The owner's standing direction
authorizes bounded automatic execution after independent review, not recurring
plan confirmation. Gauss prospectively approves this diagnostics-only scope;
concrete code review and local gates precede publication.

Entry: published 8e38d0359df84e67af76541195ebeda99226e1df, main equal to cached
origin/main. P3-042 CI 34354525791 fails the original Reader focus assertion at
run_product_e2e.py:19480. Six other required jobs pass. Two independently reviewed
owned-production prefix replays are valid NOT_REPRODUCED, with zero stderr and
strict audits, stable bindings and complete cleanup. Reader cause is UNKNOWN.
P3-042 stays OPEN / CI BLOCKED; P3-039 stays OPEN / DEFERRED. Neither is repaired
or closed by diagnostic publication. Formal v1.1.0; candidate None.

## Exact Paths

- scripts/e2e/run_product_e2e.py: one failure-only helper and the exact assertion wrapper
- backend/tests/test_reader_history_failure_diagnostics.py: offline contracts
- docs/tasks/P3-034.1_GUIDED_READER_HISTORY_FOCUS_DIAGNOSTICS.md
- docs/P3_034_1_GUIDED_READER_HISTORY_FOCUS_REPORT.md
- alignment.md
- docs/tasks/CURRENT_TASK.md
- docs/00_PROJECT_STATE.md
- roadmap.md
- docs/V1_2_ROADMAP.md
- README.md
- docs/tasks/P3-042_ARTICLE_LIST_ROUTE_STATE_CONTINUITY.md: status/evidence only
- docs/P3_042_ARTICLE_LIST_ROUTE_STATE_CONTINUITY_REPORT.md: status/evidence only

These twelve paths include the eight existing parent-owned documentation edits.
Preserve and exclude the untracked graph_frame_oracle.js, SHA-256
21b548fe1a6c7923a016053e94881c955a0a4c92b2d33fe2020fed4c8bbd9b34.

## Contract

1. Preserve the original guided_heading focus assertion, its position and
   30000-ms timeout. Add no pre-checkpoint read, wait, focus, event listener,
   retry, response delay or scheduler intervention.
2. Capture only after that AssertionError and re-raise the same exception.
   Diagnostic evaluation, serialization or add_note failure must never replace
   it, erase its original notes or admit PASS. Other exceptions remain unchanged.
3. One synchronous DOM read exports only fixed enums, strict booleans and four
   finite/null heading rectangle values. Distinguish Outline link/region, H1,
   body, main, dialog and other owners; report document.hasFocus separately
   from H1 being the active element. Include guided/hash/Reader/pending/modal
   booleans or fixed categories. Bound the serialized note.
4. No raw labels, text, HTML, URLs, headers, exception text, screenshots, trace,
   profile, console log or arbitrary attributes. Unknown output fields/types
   fail closed to a fixed capture-unavailable note, while UI failure remains.
5. Tests execute the production wrapper and projection, proving no capture on
   success, original assertion identity/timeout, privacy whitelist, bounded
   output, and capture/serialization/annotation failure propagation.
6. Passing local/remote tests qualifies the diagnostic revision only. Retain
   failed CI 34354525791 and Reader OPEN / UNKNOWN; no speculative product fix
   or P3-042 closure. A later causal repair requires separately reviewed scope.

## Gates And Delivery

- Focused offline diagnostic tests and independent concrete code review.
- uv run --offline --project backend --extra dev pytest -q
- Frontend test:articles, test:references, test:tutor and test:graph.
- Owned production build and original exclusive Product E2E:
  SCIENTIFIC_SPACES_E2E_BACKEND_PORT=18000 uv run --offline --project backend python scripts/e2e/run_product_e2e.py --repeat 3 --frontend-mode start
- Preserve every original assertion, fixture and strict request/error policy,
  including all additive image/Article navigation/component and restart gates.
- Use the existing owned build/runtime helper, synthetic fixtures and fake
  providers only. Do not stop/reuse the user backend8000 or mutate shared .next.
- Documentation consistency, artifact/secret/protected-path audits and two
  independent final reviews precede one substantive diagnostic commit:
  test: capture guided Reader history focus failure
- Non-force push main, then verify all seven required exact-SHA CI jobs and
  complete process output; Docker/release skips remain skips. Do not rerun a
  failed workflow blindly. Carry the receipt with the next genuine change;
  no self-hash receipt-only commit loop or separate diagnostic closure commit.

## Exclusions And Stops

No Frontend product, Reader, Shell, ArticleList, Graph, Backend product, M1,
API/schema, storage, persistence, canonical content/corpus, source acquisition,
private Zotero, real/paid Provider, dependencies, locks, workflows, candidate,
tag, Release or destructive Git change. No runtime/private artifact or secret
in Git. Stop the affected action on unknown drift, unsafe output, failed gate
or necessary scope expansion; diagnose with evidence rather than waive it.
