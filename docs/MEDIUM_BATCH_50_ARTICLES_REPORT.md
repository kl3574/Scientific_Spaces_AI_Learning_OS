# Medium Batch 50 Articles Report

## Current Status

- P1-004 50-Article Phase: PASS
- Target: cumulative 50 valid Article records
- Result: 50 valid Article records
- Recommendation: A: Ready for P1-005 100-article medium batch phase

This phase is the second medium-batch execution step after the P1-003.1 seed and legacy-page fix. It did not run a 100-article batch, did not run a full crawl, did not generate PDFs, and did not commit runtime corpus data.

## Scope

| Field | Value |
|---|---:|
| target_count | 50 |
| cumulative_valid_count | 50 |
| existing_runtime_count_before_P1-004 | 26 |
| newly_attempted_count_across_growth_runs | 31 |
| newly_imported_count_across_growth_runs | 24 |
| final_idempotent_attempted_count | 0 |

No full crawl confirmation:

- The phase used the approved seed list as the candidate source.
- No search page crawling was performed.
- No search engine scraping was performed.
- No archive ID range probing was performed.
- No category, tag, comment, login, attachment, static asset, or PDF paths were requested by the batch runner.

No PDF batch confirmation:

- The command did not call `ArticlePdfExporter`.
- No PDF output was generated by this phase.

No artifact commit confirmation:

- Runtime output remained under ignored `.local_data/`.
- No Article store, progress file, validation summary, failed URL log, HTML dump, browser trace/profile/cache, image, full seed list, or PDF was committed.

## Implementation Changes

Files changed:

- `backend/app/corpus/pilot.py`
- `backend/tests/test_full_corpus_pilot.py`
- `scripts/corpus/run_full_corpus_pilot.py`
- `docs/MEDIUM_BATCH_50_ARTICLES_REPORT.md`
- `docs/00_PROJECT_STATE.md`

Behavioral changes:

- The bounded corpus runner now supports `limit=50`.
- Limits above 30 require `delay_seconds >= 5`.
- The default CLI `--max-limit` is 50 for P1-004.
- The runner stops after `max_consecutive_failures=5`.
- Robots/source-policy failure preserves existing valid runtime state and reports `CONDITIONAL` instead of overwriting the summary with zero imports.
- A single failed candidate can be replaced when the cumulative target is reached; invalid imported content remains a blocker.

CLI used:

```bash
uv run --project backend python scripts/corpus/run_full_corpus_pilot.py \
  --limit 50 \
  --delay-seconds 5 \
  --seed-file /home/lkx/Downloads/kexuefm_pdf_toolkit/article_list.json
```

Resume behavior:

- The phase started from 26 existing valid runtime Articles.
- The first live growth run imported 20 additional valid Articles and preserved all valid storage despite 3 candidate fetch failures.
- A resumed live run imported 4 more valid Articles and reached 50.
- A final idempotent rerun returned `attempted_count=0`, proving the already-satisfied 50-article path did not refetch the source.

## Discovery and Canonicalization

Source method:

- Approved seed file: `/home/lkx/Downloads/kexuefm_pdf_toolkit/article_list.json`
- Seed file format: JSON object with `articles` metadata entries
- Total seed metadata entries: 1326

Canonicalization policy:

- `spaces.ac.cn/archives/{id}` is accepted.
- `kexue.fm` archive aliases are normalized to `https://spaces.ac.cn/archives/{id}`.
- Search, comment, category, tag, non-article, unsupported-host, and malformed URLs are rejected before browser access.

Live seed result:

| Metric | Value |
|---|---:|
| discovered_url_count | 1326 |
| canonical_url_count | 1326 |
| duplicate_count | 0 |
| selected_count | 50 |
| rejected_urls | 0 |

Final idempotent summary reports `discovered_url_count=50` because the local runtime store already satisfied the target and the runner did not need to reload external source candidates.

## Polite Crawling Evidence

| Setting | Value |
|---|---:|
| concurrency | 1 |
| delay_seconds | 5.0 |
| max_consecutive_failures | 5 |
| bounded retry | Browser fetcher default bounded retry |
| browser backoff | Browser fetcher default backoff |

Growth-run evidence:

| Run | attempted_count | imported_count_after_run | failed_count | elapsed_seconds |
|---|---:|---:|---:|---:|
| Growth run 1 | 27 | 46 | 3 | 547.997 |
| Growth run 2 | 4 | 50 | 0 | 92.492 |
| Idempotent rerun | 0 | 50 | 0 | 0.043 |

Transient failure classification:

- Growth run 1 recorded `browser_transient_failures=2`.
- Growth run 1 recorded `permanent_failures=1`.
- No invalid content was written.
- The resumed run reached the target without additional failures.

## Candidate Handling

| Metric | Growth run 1 | Growth run 2 | Final idempotent rerun |
|---|---:|---:|---:|
| invalid_candidate_count | 0 | 0 | 0 |
| invalid_imported_content_count | 0 | 0 | 0 |
| skipped_non_article_or_legacy_page | 0 | 0 | 0 |
| browser_transient_failures | 2 | 0 | 0 |
| permanent_failures | 1 | 0 | 0 |

Replacement behavior:

- Failed candidates did not enter storage.
- Existing valid runtime Articles remained valid.
- Replacement candidates from the approved seed list completed the cumulative target.

## Import and Validation Metrics

Final cumulative result:

| Metric | Value |
|---|---:|
| cumulative_valid_count | 50 |
| newly_attempted_count_across_growth_runs | 31 |
| newly_imported_count_across_growth_runs | 24 |
| failed_count_across_growth_runs | 3 |
| final_duplicate_count | 0 |
| final_content_completeness_rate | 1.0 |
| final_formula_valid_rate | 1.0 |
| final_metadata_completeness_rate | 1.0 |
| final_short_content_count | 0 |
| final_invalid_imported_content_count | 0 |
| final_parser_quality_issues | 0 |

Final sample Article IDs:

```text
100, 101, 102, 11328, 11335, 11340, 11371, 11388, 11390, 11404,
11416, 11428, 11459, 11469, 11480, 11486, 11494, 11530, 11540,
11549, 11563, 11578, 11593, 11605, 11619, 11626, 11647, 11654,
11664, 11673, 11693, 11697, 11710, 11719, 11729, 11736, 11738,
11750, 11760, 11767, 11772, 11777, 11782, 11784, 11787, 11804,
119, 3319, 41, 6508
```

## Resume / Checkpoint Evidence

| Field | Value |
|---|---:|
| existing_runtime_count_before_P1-004 | 26 |
| runtime_article_count_after_growth_run_1 | 46 |
| runtime_article_count_after_growth_run_2 | 50 |
| final_idempotent_attempted_count | 0 |
| resume_used | true |

Source transient handling:

- The first growth run stopped short at 46 because of candidate fetch failures.
- The runtime Article store retained all successful imports.
- The resumed run continued from the checkpoint and imported only the remaining 4 records.
- The final rerun used local runtime data only and did not refetch articles.

## Artifact Policy

Runtime output path:

```text
.local_data/scientific_spaces/corpus/pilot
```

Git ignore coverage:

- `.local_data/` is ignored.
- Runtime progress, validation summary, failed URL log, HTML snapshots/dumps, browser traces/profiles, PDFs, FAISS indexes, embedding caches, and Node/Python caches are ignored.

No committed runtime artifacts:

- No Article corpus JSON committed.
- No progress file committed.
- No validation summary committed.
- No failed URL log committed.
- No full `article_list.json` committed.
- No PDF, HTML dump, screenshot, trace, profile, cache, or image committed.

## Test Evidence

Backend:

```bash
uv run --project backend --extra dev pytest -q
```

Result:

```text
118 passed, 2 skipped
```

Frontend:

```bash
npm run build
```

Result:

```text
Compiled successfully
Generated static pages (8/8)
```

RAG/tutor evaluation:

```bash
uv run --project backend python scripts/eval/run_rag_tutor_eval.py
```

Result:

```text
Overall: PASS
```

Live command:

```bash
uv run --project backend python scripts/corpus/run_full_corpus_pilot.py \
  --limit 50 \
  --delay-seconds 5 \
  --seed-file /home/lkx/Downloads/kexuefm_pdf_toolkit/article_list.json
```

Final idempotent live result:

| Metric | Value |
|---|---:|
| status | PASS |
| target_count | 50 |
| selected_count | 50 |
| attempted_count | 0 |
| imported_count | 50 |
| duplicate_count | 0 |
| invalid_imported_content_count | 0 |
| content_completeness_rate | 1.0 |
| formula_valid_rate | 1.0 |
| metadata_completeness_rate | 1.0 |

## Risks

- Source volatility can still produce transient browser failures.
- Seed-list ordering can bias which historical articles enter each medium batch.
- Parser drift remains possible if Scientific Spaces changes legacy DOM structures.
- Playwright/Chromium runtime drift can change fetch behavior.
- Scaling from 50 to 100 increases exposure to source volatility and should keep the same bounded, resumable, single-concurrency policy.

## Recommendation

A: Ready for P1-005 100-article medium batch phase
