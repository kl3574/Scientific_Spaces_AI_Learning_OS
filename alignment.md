# P3-044 Alignment

Canonical: [Tutor mode policy and offline regression](docs/tasks/P3-044_TUTOR_MODE_POLICY_AND_OFFLINE_REGRESSION.md).
Current state: [project state](docs/00_PROJECT_STATE.md).

The owner has authorized synchronizing the necessary reviewed P3-044 work to
GitHub and removing redundant documentation. This replaces the earlier local-only
delivery boundary for this action. Preserve uncommitted work, private data and
running services; do not reset, stash, rewrite history or integrate unaudited Reader code.

The v3 affine reference packet has 12/12 explicit owner approvals recorded in the
[content-bound human decision](docs/reviews/P3_044_AFFINE_V3_HUMAN_DECISION.json).
Keep its preparation snapshot immutable. Approval covers the Articles, cases and
reference content; model-answer quality remains NOT_RUN.

Current work is limited to publication preparation, validation, documentation
deduplication and authorized GitHub synchronization. It adds no product feature,
schema, source-selection change, real-provider call, source/private-data access, tag
or Release. Existing verification and public-data safety gates remain in force;
remote outcomes must be read back for the exact published commit.

Reader P3-042 and Graph P3-039 retain the independent open states in project state.
P3-043 has local implementation and partial evidence, but no terminal full-acceptance
record; only its accurate progress and remaining verification are synchronized.

NEXT_TASK: Plan controlled-response, claim-level offline regression against the
approved v3 reference content. Do not implement that follow-on as part of this sync.

Historical scope decisions, failed runs and publication receipts remain in each
original canonical task/report and Git history. They are not repeated here.
